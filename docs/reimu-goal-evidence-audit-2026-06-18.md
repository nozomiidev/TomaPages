# Reimu Goal Evidence Audit - 2026-06-18

This audit maps the active Reimu recovery goal to current evidence on `main`. It records observable artifacts and command results only.

## Current State

- Reimu code/assets baseline audited: current main after OpenAI sleeve material width tuning pass
- GitHub Pages workflow: verify the newest pushed run in repository Actions; this document keeps durable local evidence instead of a stale run ID.
- Public Reimu output: `public/characters/reimu`
- Public frame count: 225 WebP frames
- No-reshape baseline frame count: 225 WebP frames
- Local untracked evidence area: `tmp/`
- Requirement-level automated evidence gate: `npm.cmd run verify:reimu:goal`
- Requirement-level evidence output: `tmp/goal-audit/reimu-goal-evidence-summary.json`
- Chrome GUI note: if Chrome GUI is needed, first verify the window belongs to `nozomidevbusin@gmail.com`

## Evidence Matrix

| Requirement | Evidence | Status |
| --- | --- | --- |
| Recreate or faithfully recover the interrupted OpenAI-assisted approach | `docs/reimu-quality-process-reconstruction-2026-06-17.md`, local full-preserve bundle under `tmp/recovery/reimu-quality-2026-06-17`, OpenAI reference metrics in `tmp/reference-audit` | Strong evidence |
| Keep OpenAI outputs as usable process inputs, not ignored artifacts | `tmp/reference-audit/reimu-reference-metrics.json`, `docs/reimu-openai-sleeve-candidate-2026-06-18.md`, `docs/reimu-quality-recovery-index-2026-06-18.md`, `docs/reimu-lossless-quality-report-2026-06-18.md` | Strong evidence |
| Adopt OpenAI output only through controlled local materialization | Production frames are generated by `tools/slice-fumo-assets.mjs`; fresh image-model candidates are blocked as whole-frame replacements when drift is high, but their sleeve measurements feed `metaassets/fumo/reimu/reimu_openai_sleeve_material_recipe.json` and local post-processing | Proven by source path and candidate audit |
| Regenerate Reimu 225 frames | `npm run quality:reimu` rebuilds `tmp/noreshape/reimu` and `public/characters/reimu`; `verify:reimu:quality` reports 225 public and 225 no-reshape frames | Proven by command |
| Rebuild `tmp/audit` and `tmp/compare` | `verify:reimu:quality` reports 21 audit sheets and 12 compare sheets; dimensions are checked by `scripts/verify-reimu-quality-artifacts.mjs` | Proven by command |
| Compare before/after and verify fewer artifacts | `npm run quality:reimu` now runs `audit:assets:baseline-delta`; `verify:reimu:quality` requires fresh `tmp/baseline-delta/reimu-baseline-quality-delta.csv`, `.json`, and `.png` artifacts; `docs/reimu-baseline-delta-audit-2026-06-18.md` and the summary JSON compare recovered `tmp/before-lossless` against current public frames: transparent RGB residue `567074 -> 0`, internal gap area `8052 -> 914`, detached/line-like/light-gap areas remain `0 -> 0` | Proven for measured metrics and artifacts |
| Remove transparent RGB residue that can create invisible inner contour bleed | `maxTransparentNonBlack = 0` in `tmp/quality-audit/reimu-asset-quality-summary.json`; default hard cap is 0 | Proven by audit |
| Prevent low-alpha ghost contours and hidden colored transparent residue | `tmp/edge-audit/reimu-edge-integrity-summary.json` reports `maxOrphanWeakAlphaPixels = 0` and `maxTransparentColoredPixels = 0`; `verify:reimu:quality` requires the edge-integrity overlay and metrics | Proven by audit |
| Prevent detached alpha fragments and thin stray parts | `detachedArea = 0`, `detachedSliverArea = 0`; default hard caps are 0 | Proven by audit |
| Prevent suspicious line-like transparent holes | `suspiciousHoleArea = 0`, `lineLikeHoleArea = 0`; default hard caps are 0 | Proven by audit |
| Preserve larger legitimate internal gaps instead of blindly filling everything | Internal gap cap remains explicit; issue overlay review is generated in `tmp/issues/reimu-issue-overlay.png` | Strong evidence |
| Remove small light-sleeve transparent inner holes without filling legitimate negative space | `tools/slice-fumo-assets.mjs` fills only small internal holes surrounded by light cloth pixels; `tools/audit-character-assets.mjs` requires `maxLightInteriorGapArea = 0`; `docs/reimu-light-interior-gap-audit-2026-06-18.md` records 14 components / 7340 px -> 9 components / 5333 px | Proven by audit |
| Remove post-processing internal holes that are covered by the same-frame reference | `tools/slice-fumo-assets.mjs` restores same-frame reference pixels after sleeve reshape and expression stabilization; `tools/audit-reimu-reference-covered-gaps.mjs` requires `maxReferenceCoveredGapArea = 0`; `docs/reimu-reference-covered-gap-audit-2026-06-18.md` records 4419 px -> 0 px | Proven by audit |
| Distinguish actionable residual defects from review-only residuals | `tools/audit-reimu-residual-defects.mjs` requires `actionableDefectFrameCount = 0`; `tmp/quality-audit/reimu-residual-defect-summary.json` records 225 frames, actionable defects 0, and one original internal-gap review candidate | Proven by audit |
| Check visible contour support around alpha edges | `tools/audit-reimu-line-integrity.mjs` checks 225 frames for alpha-edge pixels without nearby ink/trim support and now groups those pixels into connected components so long contour-break candidates cannot hide inside a low total count; latest max unsupported edge ratio is `0.0467`, max unsupported component span is `36`, below the `0.055` and `42` caps | Proven by audit |
| Reduce talk/blink flicker from body-wide expression changes | `tools/slice-fumo-assets.mjs` stabilizes Reimu expression sheets against base-pose body pixels; `tmp/expression-audit/reimu-expression-diff-audit-summary.json` records max changed pixels `45472 -> 13797`, max alpha-changed pixels `13948 -> 1409`, and expression-region leakage caps `913 / 1100 px`, `0.0742 / 0.08` | Strong evidence |
| Avoid head/leg clipping | Minimum margin is 81px in the current audit summary, above the 32px hard floor; browser screenshots show Reimu visible in checked pose | Strong evidence, sampled visually |
| Provide a full visual sweep for every shipped Reimu frame | `tmp/sweep/reimu-full-sweep-pink.png`, `tmp/sweep/reimu-full-sweep-dark.png`, and `tmp/sweep/reimu-full-sweep-alpha.png` are generated by `audit:assets:sweep` and verified by `verify:reimu:quality` | Proven by artifact verifier |
| Add broader drawing-level anomaly evidence beyond alpha/mask checks | `tools/audit-reimu-perceptual-consistency.mjs` combines current/no-reshape/diff review candidates from weak alpha, internal gaps, expression deltas, neighbor jumps, sleeve guards, and OpenAI target rows; it also emits `tmp/perceptual-audit/reimu-perceptual-candidate-zooms.png` for larger current/no-reshape visual acceptance and `tmp/perceptual-audit/reimu-perceptual-candidate-disposition.json` to prove candidate-level `review-only` disposition; `verify:reimu:quality` requires the zoom sheet, disposition rows, `actionableCandidateCount = 0`, `severeIssueCount = 0`, the expression-region leakage hard check, and the neighbor-stability hard check | Strong evidence |
| Use fresh OpenAI image-model output in the resumed approach | Built-in image-model sleeve edits were generated for `cy_01/r0c2` and `ct_01/r1c2`, copied into `tmp/imagegen/reimu-sleeve-candidates`, measured at sleeve ratios `0.231` and `0.233`, included in reference metrics when present, promoted into the OpenAI target sheet, converted into alpha/normalized/projected sleeve material artifacts by `npm run audit:assets:openai-sleeve-candidates`, and summarized into `metaassets/fumo/reimu/reimu_openai_sleeve_material_recipe.json` | Strong local evidence |
| Materialize OpenAI candidates without replacing the whole frame | `tmp/imagegen/reimu-sleeve-candidates/processed/reimu-openai-sleeve-candidates-summary.json` records high non-sleeve drift, `controlledMaterialAllowed = true`, and `materialization.adoptionMode = sleeve-mask-and-proportion-material`; `verify:reimu:quality` requires those fields, and the slicer logs `OpenAI-derived material bounds` during regeneration | Proven by candidate preprocessing and verifier |
| Prove OpenAI material changes the shipped frames in a scoped way | `npm run build:assets:reimu:no-openai-material` builds a no-material Reimu baseline under `tmp/openai-material-baseline`; `npm run audit:assets:openai-material` compares it with `public/characters/reimu` and writes `tmp/openai-material-audit/reimu-openai-material-application-summary.json`, `.csv`, and `.png`; the current audit records 25 changed frames, all in T/Y target sheets, with preserved margins, average sleeve-width material signal `0.0176 / 0.017`, and bounded outside-sleeve diff `0.0755 / 0.08` | Proven by frame-level material audit |
| Prevent OpenAI sleeve material regression | `verify:reimu:quality` and `verify:reimu:goal` now require the tuned recipe floor `0.285`, at least 25 scoped OpenAI-material changed frames, outside-sleeve diff `<= 0.08`, average sleeve-width signal `>= 0.017`, and max T/Y side-width loss `<= 0.101`; this catches a return to the older weaker 16-frame material pass | Proven by artifact and goal verifiers |
| Avoid T/Y separate sleeve-overlay asset approach | Sleeve edits are applied into generated WebP frames by `tools/slice-fumo-assets.mjs`; no separate sleeve overlay is shipped | Proven by source path |
| Keep T/Y sleeve area close to plain-pose proportions | `tools/audit-reimu-sleeve-guards.mjs` and `tmp/quality-audit/reimu-sleeve-guard-summary.json` enforce width floors and loss limits; `tools/slice-fumo-assets.mjs` rejects frame-wide sleeve reshapes that create audit-compatible side-balance regressions and now reads the OpenAI-derived material recipe for conservative T/Y sleeve-width floors; latest max side imbalance is `0.1530`, max side width loss improved to `0.1000 / 0.12`, and `ct_01/r1c2.webp` remains at `0.0976` | Proven by audit |
| Reduce perceived center-of-mass jitter in live direction changes | `targetToStableCell` is covered by unit tests and deployed browser pointer checks; `tmp/quality-audit/reimu-asset-quality-summary.json` now records direction-neighbor thresholds and `verify:reimu:goal` requires max neighbor center, alpha, width, and height steps to remain within those limits; latest max neighbor center step is `15.21 / 32` | Proven by runtime and frame-level audits |
| Verify static GitHub Pages site still works | `npm run check`, `npm run verify:pages`, deployed browser check on Talk page, local preview screenshots under `tmp/browser-screenshots/reimu-pose-{p,t,y}.png`, and GitHub Pages workflow success after pushed quality passes | Proven for checked pages/build |
| Keep the active goal resumable by future Codex threads | `scripts/verify-reimu-goal-evidence.mjs` reads the current audit outputs and writes `tmp/goal-audit/reimu-goal-evidence-summary.json`; the current gate passes 22/22 automated requirements while preserving the explicit caveat that final art acceptance still depends on visual review | Proven by command |

## Commands Verified In This Pass

```bash
npm.cmd run verify:reimu:quality
npm.cmd run quality:reimu
npm.cmd run audit:assets:edge
npm.cmd run audit:assets:lines
npm.cmd run audit:assets:expression
npm.cmd run audit:assets:reference-gaps
npm.cmd run audit:assets:residuals
npm.cmd run audit:assets:perceptual
npm.cmd run audit:assets:baseline-delta
npm.cmd run audit:assets:openai-sleeve-candidates
npm.cmd run build:assets:reimu:no-openai-material
npm.cmd run audit:assets:openai-material
npm.cmd run audit:assets:sweep
npm.cmd run verify:reimu:goal
npm.cmd run check
npm.cmd run test
npm.cmd run lint
```

Additional verified evidence from the frame-level sleeve guard pass:

```bash
npm.cmd run build:assets:fumo -- --characters reimu --lossless --quality 100
npm.cmd run quality:reimu
npm.cmd run check
```

Additional verified evidence from the perceptual candidate disposition gate pass:

```bash
npm.cmd run audit:assets:perceptual
npm.cmd run verify:reimu:quality
npm.cmd run quality:reimu
npm.cmd run check
```

Recent verified commands from the immediately preceding stability pass:

```bash
npm.cmd run test -- src/domain/character.test.js
npm.cmd run lint
npm.cmd run check
```

## Completion Assessment

The Reimu-specific technical recovery has strong evidence across frame count, artifact regeneration, transparent RGB cleanup, weak-alpha ghost contour detection, expression-frame body stabilization with outside-region leakage caps, detached-fragment cleanup, line-hole cleanup, sleeve regression guards, direction stability, full-frame visual sweep artifacts, perceptual consistency candidates, and deployed Pages behavior.

The new `verify:reimu:goal` gate currently passes 22/22 automated requirements and records the result in `tmp/goal-audit/reimu-goal-evidence-summary.json`. It includes the baseline-delta artifact gate, the expression-region leakage gate, the OpenAI material application and regression-headroom audits, and the direction-neighbor stability gate so the recovery state is reproducible from local artifacts instead of depending on scattered notes.

The full active goal should remain open because "product-level quality" and "all visual breakage" still require either user acceptance of the full sweep/contact sheets plus perceptual candidate sheet, or a stronger automated drawing-consistency detector than the current heuristic review gate. The next strongest completion evidence would be explicit visual acceptance of the generated sweep and perceptual sheets, or an automated detector that can classify drawing-level inconsistency rather than only collecting the highest-risk candidates.
